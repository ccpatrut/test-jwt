quarkus.log.level=INFO
quarkus.http.test-port=8083
#3scale
threescale.instance.config.Develop.host-name=localhost
threescale.instance.config.Acceptance.host-name=localhost
threescale.instance.config.Production.host-name=localhost
threescale.instance.config.Develop.secret=secret
threescale.instance.config.Acceptance.secret=secret
threescale.instance.config.Production.secret=secret
quarkus.keycloak.policy-enforcer.enable=false
threescale.develop.secret=test
threescale.acceptance.secret=test
threescale.production.secret=test
quarkus.oidc.credentials.secret=test
threescale.user.default.password=test
threescale.ssl.port=1443
instance.proxy-settings=none
#jks config
vertx.truststore.path=client.jks
vertx.truststore.secret=changeit
#test config
quarkus.oidc.application-type=service
keycloak.url=http://test:8080
quarkus.oidc.api-spec.auth-server-url=${keycloak.url}/realms/quarkus/
quarkus.oidc.api-spec.credentials.secret=test
quarkus.oidc.auth-server-url=${keycloak.url}/realms/quarkus/
quarkus.datasource.reactive.url=${TEST_DB_URL:postgresql://localhost:5432/postgres}
quarkus.datasource.jdbc.url=jdbc:${TEST_DB_URL:postgresql://localhost:5432/postgres}
quarkus.datasource.reactive.trust-all=true
quarkus.datasource.reactive.postgresql.ssl-mode=${TEST_DB_SSL_MODE:DISABLE}
quarkus.datasource.username=${TEST_DB_USER:postgres}
quarkus.datasource.password=${TEST_DB_PASSWORD:postgres}
quarkus.datasource.jdbc=${ENABLE_TEST_DB:false}
quarkus.datasource.db-kind=postgresql
threescale.oidc.issuer.endpoint=https://3scale-api:hahasecret@d-api-sso.vt.ch/auth/realms/3scale-api
quarkus.mailer.mock=true
